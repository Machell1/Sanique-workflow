appId: dev.sanique.workspace
productName: Sanique's workspace
copyright: Copyright (c) 2026 Sanique Richards

directories:
  output: release
  buildResources: build

files:
  - dist/**/*
  - electron/**/*
  - public/**/*
  - "package.json"
  - "!**/*.{ts,tsx,map,md}"
  - "!{tsconfig.json,tsconfig.node.json,vite.config.ts,tailwind.config.js,postcss.config.js,electron-builder.yml,.gitignore}"
  - "!legacy${/*}"
  - "!src${/*}"

asarUnpack:
  - "node_modules/better-sqlite3/**/*"

extraResources:
  - from: public
    to: public
    filter: ["**/*"]

win:
  target:
    - target: nsis
      arch:
        - x64
  icon: build/icon.ico
  artifactName: "Saniques-Workspace-Setup-${version}.${ext}"
  publisherName: "Sanique Richards"
  requestedExecutionLevel: asInvoker

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: always
  createStartMenuShortcut: true
  shortcutName: Sanique's workspace
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
  deleteAppDataOnUninstall: false
  runAfterFinish: true
  artifactName: "Saniques-Workspace-Setup-${version}.${ext}"
  uninstallDisplayName: "Sanique's workspace v${version}"
  license: build/license.txt
  include: build/installer.nsh

publish: null

asar: true
compression: maximum

extraMetadata:
  main: electron/main.cjs
